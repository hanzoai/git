# hanzoai/git is a LIBRARY subsystem welded into the cloud binary — it ships no
# image of its own (the cloud image carries it). CI just builds + tests the module.
test:
  - go build ./...
  - go test ./...
